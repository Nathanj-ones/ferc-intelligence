# Deeper review — 11 September 2026

## Scope and fixes

- Reviewed the operating snapshot loader, export projection, metric selection,
  comparisons, source evidence, routing, project/operating boundaries and
  responsive design. The frozen backend release was not edited.
- Directory headlines now match the first safe regime-specific key metric on
  the asset page, including its precise period. No arbitrary registry ordering
  or structured record is promoted.
- Removed 38 duplicate plotted occurrences across 20 liquids assets. Deduplication
  requires identical filed source identity and every substantive observation
  field. All underlying records are retained; conflicting periods fail closed.
- Shipper-concentration comparisons now require a shared evidenced denominator
  concept and unit. Arlington transport MDQ and Cadeville storage inventory
  cannot be presented as the same measure. Labels across cards, explorer,
  evidence and comparisons distinguish storage quantity from daily capacity.
  The same guard covers affiliate share and shipper-identity coverage. A scan
  of all 408 eligible backend groups identified six mixed-denominator groups
  across these three measures; all points, not just the latest, are checked.
- Evidence retains raw filed values independently of stored interpretations.
  This restores access to structured Form 549D headers, including Banquete and
  Copano. Absent scope contracts no longer render as `null`.
- Browser Back/Forward restores URL-backed filters. Blocked local storage no
  longer breaks Changes filters or the temporary review marker.
- A stale manifest gets one verified retry. When it identifies a new frontend
  projection, the same page URL reloads coherently instead of combining an old
  directory with new details. Persistent corruption is still rejected.
- Roadrunner is displayed as a cross-border gas pipeline, while its backend
  Section 3 processing template and original evidence remain unchanged.
- Simplified the snapshot banner and added a data-status guide. Key-card grids
  fill the available width. Fixed mobile explorer overflow and expanded source
  drawers to the screen width, with an accessible close button in their sticky
  header. Categorical series use a compact explanation instead of an empty
  chart-sized panel.

## Verification

- Snapshot tests cover all 120 asset routes, hashes, 1,238 historical events,
  seven annotations, safe default metrics, comparison eligibility and complete
  lineage references.
- New regression checks cover duplicate/conflicting source occurrences, raw
  evidence preservation, mixed-denominator rejection, Roadrunner labeling,
  publication updates and persistent corruption.
- Frontend formatting/contract tests, lint and TypeScript checks pass.
- Browser checks included default key metrics, Banquete source-header disclosure,
  source-drawer closing, Back restoring company/search filters, and narrow-screen
  explorer sizing. At 390 px the corrected explorer no longer expands the page
  horizontally.
- Independent read-only backend audit verified all 257 publication-receipt file
  hashes; 44 offline Form 549D, 19 contract, six export-character and four
  migration/atomic-publication checks passed.

## Remaining data boundaries

The operating data remains a candidate snapshot dated 7 September, not a live
ingestion service. The supplied release reports 77.16% validated due-core
coverage, with 1,242 unimplemented slots, 172 retrieval failures and 278
interpretation-blocked slots. These are source/pipeline limitations, not values
to fabricate or automatically mark reviewed. No source filings were re-fetched
or unresolved source anomalies adjudicated during this review.

Project data is still the separately supplied project snapshot. No unsupported
asset-to-project mapping, parent-company aggregation or live feed was inferred.
